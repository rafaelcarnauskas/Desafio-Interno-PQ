import pandas as pd

df = pd.read_csv("ibov_2010_2025.csv", skiprows=[1])

# se quiser usar só a coluna de fechamento, ajuste o nome se necessário
prices = df["Close"]

# 1. retornos
returns = prices.pct_change()

# 2. volatilidade histórica
vol = returns.rolling(21).std()

# 3. volatilidade da volatilidade
vov = vol.rolling(21).std()

# por enquanto sem retornar nada
print (vov)